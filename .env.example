# Environment Configuration Example
# Copy this file to .env for local development
# DO NOT commit .env to git - it's already in .gitignore

# ===========================================
# Firebase Configuration (REQUIRED)
# ===========================================

# For local development: Path to your Firebase service account key JSON file
# This file should be in the backend directory
# Download from: Firebase Console > Project Settings > Service Accounts > Generate New Private Key

# For Vercel/Production deployment:
# Don't use the file path - instead, set FIREBASE_CREDENTIALS in Vercel dashboard
# Copy the ENTIRE contents of serviceAccountKey.json as a JSON string

# Example: The code will automatically use serviceAccountKey.json if it exists locally
# No need to set this variable for local development if you have the file


# ===========================================
# Authentication Configuration (RECOMMENDED)
# ===========================================

# JWT Secret Key - CRITICAL for production security
# Generate a secure random key for production:
# python -c "import secrets; print(secrets.token_urlsafe(32))"

SECRET_KEY=your-super-secret-key-here-change-in-production

# ===========================================
# CORS Configuration (RECOMMENDED)
# ===========================================

# Comma-separated list of allowed origins for CORS
# Use "*" for development only (allows all origins)
# In production, specify your actual frontend URL(s)

# Development:
ALLOWED_ORIGINS=*

# Production example:
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com


# ===========================================
# Vercel Deployment Instructions
# ===========================================

# In Vercel Dashboard:
# 1. Go to your project settings
# 2. Navigate to "Environment Variables"
# 3. Add the following variables:

# FIREBASE_CREDENTIALS (REQUIRED):
#    - Type: Secret
#    - Value: Paste the ENTIRE contents of your serviceAccountKey.json file as a single-line JSON string
#    - Example: {"type":"service_account","project_id":"...","private_key_id":"..."}

# SECRET_KEY (RECOMMENDED):
#    - Type: Secret  
#    - Value: A secure random string (generate with: python -c "import secrets; print(secrets.token_urlsafe(32))")

# ALLOWED_ORIGINS (RECOMMENDED):
#    - Type: Plain Text
#    - Value: Your production frontend URL(s), comma-separated
#    - Example: https://yourdomain.vercel.app

# Note: Make sure to add these variables to all environments (Production, Preview, Development)
