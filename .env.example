# Environment Configuration Example
# Copy this file to .env for local development
# DO NOT commit .env to git - it's already in .gitignore

# ===========================================
# MongoDB Configuration (REQUIRED)
# ===========================================

# MongoDB Atlas Connection String
# Get this from: MongoDB Atlas → Database → Connect → Connect your application
# Format: mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority

MONGODB_URI=mongodb+srv://your_username:your_password@cluster0.xxxxx.mongodb.net/college_server?retryWrites=true&w=majority

# ===========================================
# Authentication Configuration (RECOMMENDED)
# ===========================================

# JWT Secret Key - CRITICAL for production security
# Generate a secure random key for production:
# python -c "import secrets; print(secrets.token_urlsafe(32))"

SECRET_KEY=your-super-secret-key-here-change-in-production

# ===========================================
# CORS Configuration (RECOMMENDED)
# ===========================================

# Comma-separated list of allowed origins for CORS
# Use "*" for development only (allows all origins)
# In production, specify your actual frontend URL(s)

# Development:
ALLOWED_ORIGINS=*

# Production example:
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com


# ===========================================
# MongoDB Atlas Setup Instructions
# ===========================================

# 1. Sign up at https://www.mongodb.com/cloud/atlas
# 2. Create a FREE M0 Cluster (512MB storage)
# 3. Create a database user:
#    - Go to: Database Access
#    - Click: Add New Database User
#    - Choose: Password authentication
#    - Set username and password (note these down)
#    - Grant role: Read and write to any database
# 
# 4. Whitelist your IP (or allow all for development):
#    - Go to: Network Access
#    - Click: Add IP Address
#    - For development: Click "Allow Access from Anywhere" (0.0.0.0/0)
#    - For production: Add your Vercel IP ranges
#
# 5. Get connection string:
#    - Go to: Database → Connect
#    - Choose: Connect your application
#    - Choose: Python, version 3.12 or later
#    - Copy the connection string
#    - Replace <password> with your actual password
#    - Replace <database> with: college_server


# ===========================================
# Vercel Deployment Instructions
# ===========================================

# In Vercel Dashboard:
# 1. Go to your project settings
# 2. Navigate to "Environment Variables"
# 3. Add the following variables:

# MONGODB_URI (REQUIRED):
#    - Type: Secret
#    - Value: Your full MongoDB connection string from Atlas
#    - Example: mongodb+srv://user:pass@cluster0.xxxxx.mongodb.net/college_server?retryWrites=true&w=majority

# SECRET_KEY (RECOMMENDED):
#    - Type: Secret  
#    - Value: A secure random string (generate with: python -c "import secrets; print(secrets.token_urlsafe(32))")

# ALLOWED_ORIGINS (RECOMMENDED):
#    - Type: Plain Text
#    - Value: Your production frontend URL(s), comma-separated
#    - Example: https://yourdomain.vercel.app

# Note: Make sure to add these variables to all environments (Production, Preview, Development)


# ===========================================
# Local Development
# ===========================================

# For local testing:
# 1. Copy this file to .env
# 2. Replace the MONGODB_URI with your actual connection string
# 3. Run: uvicorn backend.main:app --reload
# 4. Test: http://localhost:8000/api/health
